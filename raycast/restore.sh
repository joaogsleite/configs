#!/usr/bin/env bash

if [ "$(uname)" == "Darwin" ]; then
  echo "Open RayCast (cmd+space) => Import Preferences & Data"
  echo "Choose rayconfig file on this folder"
else
  echo "use ULauncher on Linux" 
fi
