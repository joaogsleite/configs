#!/usr/bin/env bash

if [ "$(uname)" == "Darwin" ]; then
  echo "Open RayCast (cmd+space) => Export Settings & Data"
  echo "Copy rayconfig file to this folder"
else
  echo "use ULauncher on Linux" 
fi
